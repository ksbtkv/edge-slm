[pytest]
testpaths = tests
python_files = test_*.py
markers =
    slow: exercises real sample files and may load external extraction models
    media: exercises audio/video transcription paths
    optional_dependency: requires optional ingestion dependencies
